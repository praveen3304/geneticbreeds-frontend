import { useEffect } from "react"; import { useNavigate } from "react-router-dom"; export default function RefundPolicy() { const navigate = useNavigate(); useEffect(() => navigate("/terms#refund"), []); return null; }
