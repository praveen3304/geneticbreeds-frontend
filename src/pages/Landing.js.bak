import React, { useEffect } from "react";

export default function Landing({ onLogin, onRegister }) {
useEffect(() => {
document.body.classList.add("landing-page");
    return () => document.body.classList.remove("landing-page");
  }, []);  

return (
    <div
      style={{
        minHeight: "100vh",
        background: "#ffffff",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        overflow: "hidden",
        padding: "20px",
        boxSizing: "border-box",
        position: "relative",
      }}
    >

      {/* Main Content */}
      <div
        style={{
          position: "relative",
          zIndex: 5,
          display: "flex",
          flexDirection: "column",
          alignItems: "center",
          textAlign: "center",
          width: "100%",
          width: "min(700px, 90vw)"
        }}
      >
        {/* Top Tagline */}
        <p
          style={{
            margin: "0 0 12px 0",
            fontSize: "clamp(16px, 3.5vw, 20px)",
            fontWeight: "850",
            letterSpacing: "2px",
            textTransform: "uppercase",
            background:
              "linear-gradient(135deg, #b91c1c 0%, #7c3aed 100%)",
            WebkitBackgroundClip: "text",
            WebkitTextFillColor: "transparent",
            fontFamily: "Georgia, serif",
          }}
        >
          The World's Most Trusted Pet Marketplace.
        </p>

        {/* Logo + Text */}
        <div
          style={{
            display: "flex",
            flexDirection: "column",
            alignItems: "center",
            marginBottom: "0px",
          }}
        >
          {/* Main Logo */}
          <img
            src="/final-logo.png"
            alt="Logo"
            style={{
              width: "clamp(140px, 55vw, 290px)",
              height: "auto",
              marginBottom: "5px",
            }}
          />

          {/* Text Image */}
          <img
            src="/logo-text.png"
            alt="Logo Text"
            style={{
              width: "clamp(200px,55vw, 380px)",
              height: "auto",
            }}
          />
        </div>

        {/* Buttons style */}
        <style>{`
          .gb-login-btn {
            padding: 11px 26px;
            border-radius: 50px;
            border: 1.5px solid rgba(185,28,28,0.3);
            background: transparent;
            color: #b91c1c;
            font-size: 14px;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.2s ease;
            font-family: sans-serif;
          }

          .gb-login-btn:hover {
            background: rgba(185,28,28,0.05);
          }

          .gb-register-btn {
            padding: 11px 26px;
            border-radius: 50px;
            border: none;
            background: linear-gradient(135deg, #b91c1c 0%, #dc2626 100%);
            color: #fff;
            font-size: 14px;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.2s ease;
            box-shadow: 0 4px 20px rgba(185,28,28,0.3);
            font-family: sans-serif;
          }

          .gb-register-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 28px rgba(185,28,28,0.4);
          }
        `}</style>

        {/* Divider */}
        <div
          style={{
            width: "48px",
            height: "2px",
            background:
              "linear-gradient(90deg, transparent, #b91c1c, transparent)",
            borderRadius: "999px",
            margin: "16px auto",
          }}
        />

        {/* CTA Buttons */}
        <div
          style={{
            display: "flex",
            gap: "12px",
            justifyContent: "center",
            flexWrap: "wrap",
            marginBottom: "10px",
          }}
        >
          <button className="gb-login-btn" onClick={onLogin}>
            Login
          </button>
          <button className="gb-register-btn" onClick={onRegister}>
            Register →
          </button>
        </div>

{/* Description */}
        <p
          style={{
            margin: "4px 0 0",
            fontSize: "clamp(10px, 3vw, 16px)",
            fontWeight: "600",
            color: "#383847",
            lineHeight: 1.6,
            maxWidth: "500px",
            fontFamily: "'Playfair Display', serif"
          }}
        >
          Connecting Pet Lovers with Verified Breeders for Rare, Exotic & Purebreed Animals Across the Globe.
        </p>


        {/* Pills BELOW buttons */}
        <div
          style={{
            display: "flex",
            flexWrap: "wrap",
            alignItems: "center",
            justifyContent: "center",
            gap: "8px",
            marginTop: "6px",
          }}
        >
          {["✦ Verified Sellers", "✦ Trusted Listings", "✦ Global Reach"].map(
            (label, i) => (
              <div
                key={i}
                style={{
                  padding: "7px 16px",
                  borderRadius: "14px",
                  background: "#ffffff",
                  border: "1px solid #e5e7eb",
                  color: "#D9A757",
                  fontSize: "12px",
                  fontWeight: "700",
                  boxShadow: "0 4px 12px rgba(0,0,0,0.05)",
                  fontFamily: "sans-serif",
                }}
              >
                {label}
              </div>
            )
          )}
        </div>
      </div>
    </div>
  );
}
